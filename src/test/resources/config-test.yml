# Application specific config

mongo:
  uri: mongodb://localhost:27017/test
  clientPoolSize: 1

# Dropwizard config

server:
  applicationConnectors:
  - type: http
    port: 9000
  adminConnectors:
  - type: http
    port: 9001

logging:
  level: WARN
  loggers:
    org.tddblog.calculator: INFO
  appenders:
    - type: console
